{
  "name": "atlas-jev-docs",
  "version": "0.1.0",
  "private": true,
  "description": "Development tools for the static document explorer",
  "scripts": {
    "format": "prettier --write jevdocs/web",
    "format:check": "prettier --check jevdocs/web"
  },
  "devDependencies": {
    "prettier": "3.9.8"
  }
}
